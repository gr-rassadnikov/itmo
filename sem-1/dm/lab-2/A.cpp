#include <iostream>
#include <algorithm>
#include <map>

#include <vector>
 using namespace  std;
 int main(){
     int n;
     vector <pair<int,int>> a(n);
     map <int,int> d;

     for (int i=0;i<n;i++){
         cin >> a[i].first;
         a[i].second = 0;
     }
     while (!a.empty()){
         for
     }
     for

}